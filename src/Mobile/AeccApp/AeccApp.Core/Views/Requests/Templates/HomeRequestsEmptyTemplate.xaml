<?xml version="1.0" encoding="UTF-8" ?>
<ContentView
    x:Class="AeccApp.Core.Views.Templates.HomeRequestsEmptyTemplate"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:ctrl="clr-namespace:AeccApp.Core.Controls">

    <StackLayout>

        <ctrl:CustomImage HorizontalOptions="Center" SourcePlatform="Ico_cruz" />

        <Label
            Margin="0,35,0,0"
            HorizontalTextAlignment="Center"
            Style="{StaticResource DarkGreyTitleStyle}"
            Text="{Binding [AllYourRequestsListOops]}" />
        <Label
            Margin="0,20"
            HorizontalTextAlignment="Center"
            Style="{StaticResource DarkGreyMediumLabelStyle}"
            Text="{Binding [AllYourRequestsListNoHomeRequests]}" />
    </StackLayout>

</ContentView>